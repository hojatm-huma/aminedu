{{- range .Values.configmaps }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
  labels:
    {{- include "helmchart.labels" $ | nindent 4 }}
data:
  {{- toYaml .data | nindent 2 }}
---
{{- end }}
